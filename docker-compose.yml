services:
  backend:
    build:
      context: ./backend_core_graph
      dockerfile: Dockerfile
    ports:
      - "5000:5000"

  frontend:
    build:
      context: ./gui
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    depends_on:
      - backend
